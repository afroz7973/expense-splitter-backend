{
  "build": {"builder": "NIXPACKS"},
  "deploy": {
    "startCommand": "python manage.py migrate && uvicorn core.asgi:application --host 0.0.0.0 --port $PORT --ws websockets",
    "restartPolicyType": "ON_FAILURE"
  }
}